apiVersion: v1
kind: Service
metadata:
  name: {{ include "axiom-ui.fullname" . }}
  labels:
    {{- include "axiom-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "axiom-ui.selectorLabels" . | nindent 4 }}
